name: deusmagnus
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ✅ Firebase dependencies (all versions compatible)
  firebase_core: ^3.10.0
  firebase_auth: ^5.3.1
  introduction_screen: ^3.1.12
  cloud_firestore: ^5.5.1
  firebase_storage: ^12.3.4

  # ✅ Image picker for profile photos
  image_picker: ^1.1.2

  # ✅ Shared preferences and icons
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2
  font_awesome_flutter: ^10.10.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
